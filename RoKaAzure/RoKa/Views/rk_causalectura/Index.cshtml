@model IEnumerable<RoKa.Models.rk_causalectura>

@{
    ViewBag.Title = "Index";
}

<h2>Causa de Lectura</h2>

<p>
   @* @Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.rk_servicio.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rk_servicio.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @*
            @Html.ActionLink("Edit", "Edit", new { id=item.idcausalectura }) |
            @Html.ActionLink("Details", "Details", new { id=item.idcausalectura }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idcausalectura })
            *@
        </td>
    </tr>
}

</table>
