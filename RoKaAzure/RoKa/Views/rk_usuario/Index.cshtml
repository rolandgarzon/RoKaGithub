@model IEnumerable<RoKa.Models.rk_usuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.rk_departamento.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rk_pais.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombrecompleto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idmunicipio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rk_departamento.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rk_pais.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombrecompleto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idmunicipio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idusuario }) |
            @Html.ActionLink("Details", "Details", new { id=item.idusuario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idusuario })
        </td>
    </tr>
}

</table>
