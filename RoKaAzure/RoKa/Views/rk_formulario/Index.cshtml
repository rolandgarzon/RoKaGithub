@model IEnumerable<RoKa.Models.rk_formulario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idformulario }) |
            @Html.ActionLink("Details", "Details", new { id=item.idformulario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idformulario })
        </td>
    </tr>
}

</table>
