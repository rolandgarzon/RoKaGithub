@model IEnumerable<RoKa.Models.rk_observacioneslectura>

@{
    ViewBag.Title = "Index"; 
}

<h2>Observaciones de Lectura</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.rk_servicio.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rk_tipocobroobervacion.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rk_tipoobervacionlectura.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rk_servicio.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rk_tipocobroobervacion.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rk_tipoobervacionlectura.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idobservacionlectura }) |
            @Html.ActionLink("Details", "Details", new { id=item.idobservacionlectura }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idobservacionlectura })
        </td>
    </tr>
}

</table>
